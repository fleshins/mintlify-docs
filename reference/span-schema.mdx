---
title: 'Reference: Span Schema'
description: 'Brixo consumes these fields when present'
---

## OpenAI spans
- `gen_ai.system`, `gen_ai.model`, `gen_ai.request.type`
- `gen_ai.prompt.*`, `gen_ai.completion.*`
- `gen_ai.usage.prompt_tokens`, `gen_ai.usage.completion_tokens`, `gen_ai.usage.total_tokens`

## HTTP/tool spans
- `http.method`, `http.url`, `http.status_code`

## Semantic steps (optional)
- `workflow.name`, `workflow.version`
- `traceloop.span.kind` ∈ `workflow|task|agent|tool`

If semantic spans are absent, Brixo infers steps using parent/child links and timing.
